{
    "name": "fullstackapp",
    "version": "1.0.0",
    "description": "Fullstack portfolio app",
    "main": "server/server.js",
    "scripts": {
      "deploy": "git pull origin main && npm install && pm2 start 'npm run start'",
      "install": "cd client && npm install && cd ../server && npm install",
      "start": "cd client && npm run build && cd ../server && node server"
    },
    "license": "ISC",
    "dependencies": { }
  }
  