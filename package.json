{
    "name": "fullstackapp",
    "version": "1.0.0",
    "description": "Fullstack portfolio app",
    "main": "server/server.js",
    "scripts": {
      "deploy": "git pull origin main && npm install && pm2 startOrRestart 'npm run start'",
      "install": "cd client && npm install && cd ../server && npm install",
      "start": "cd client && npm run build && cd ../server && node server"
    },
    "license": "ISC",
    "engines": {
      "node": "16"
    },
    "dependencies": {
      "express": "^4.17.3"
    }
  }
  